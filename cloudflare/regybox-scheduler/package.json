{
  "name": "regybox-scheduler",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "deploy": "node scripts/render-wrangler-config.mjs && wrangler deploy -c .wrangler.deploy.jsonc"
  },
  "dependencies": {
    "worker-mailer": "^1.1.5"
  },
  "devDependencies": {
    "wrangler": "^4"
  },
  "cloudflare": {
    "bindings": {
      "REGYBOX_STATE": {
        "description": "Created automatically to remember which classes have already been handled."
      },
      "PHPSESSID": {
        "description": "Copy this cookie value from regybox.pt developer tools so the Worker can sign in."
      },
      "REGYBOX_USER": {
        "description": "Copy this cookie value from regybox.pt developer tools so the Worker can identify your account."
      },
      "CALENDAR_URL": {
        "description": "Paste the secret iCal address from your Google Calendar settings."
      },
      "EMAIL_USERNAME": {
        "description": "Optional Gmail address used to send notifications."
      },
      "EMAIL_PASSWORD": {
        "description": "Optional Gmail app password used to send notifications."
      },
      "EMAIL_TO": {
        "description": "Optional email address that receives notifications."
      },
      "CALENDAR_EVENT_NAMES": {
        "description": "Calendar event title or titles that mean a class, separated by commas."
      },
      "CLASS_TYPE": {
        "description": "Exact Regybox class name or names to use as a fallback, separated by commas."
      },
      "TIMEZONE": {
        "description": "Timezone used to interpret class times."
      },
      "LOOKAHEAD_HOURS": {
        "description": "How many hours ahead the Worker looks for classes."
      }
    }
  }
}
